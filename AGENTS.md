# Real Provider Resource E2E

For a user request containing the exact text `real-resource-e2e`, include the marker `AGENTS_E2E_OK` in the final response. When an explicitly selected skill also requires a marker, preserve both markers in the final response.

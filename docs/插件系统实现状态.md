| 编号/功能 | 当前状态 | 已实现功能 | 未实现或必须补齐 | 下一步与完成标准 | 主要代码/测试证据 |
| --- | --- | --- | --- | --- | --- |
| 总体插件系统 | `partial` | 静态插件 runtime、Agent middleware、coding-agent 宿主、Profile、frontend 基础、子 Agent、动态 broker 生命周期和 Web 投影均已存在。 | 阶段 2 的 manifest 收口、阶段 8 的动态跨进程能力协议、阶段 9 的真实 Web frontend 尚未完成。 | 只能按本表“下一步”逐项执行；未完成项不得标记 `complete`。 | `docs/插件系统扩展实施方案.md` 第 9～10 节 |
| 阶段 0：基础包与边界 | `complete` | `@di-code/plugin-runtime` workspace、根入口、Plugin API、scope、贡献快照、独立构建和生产 runtime loader 已存在。 | 无本阶段已知主缺口。manifest 校验重复实现归入阶段 2 收口。 | 保持跨包只使用公共根入口，不导入其他包的 `src`/`dist` 私有路径。 | `packages/plugin-runtime/src/index.ts`；`packages/plugin-runtime/test/root-entry.test.ts` |
| 阶段 1：Agent 请求快照与 middleware | `complete` | 每轮 Provider 请求解析 context provider；工具调用使用请求快照和 middleware 链；取消、工具结果和下一轮请求由统一 Agent Loop 管理。 | 无本阶段已知主缺口。 | 不在插件、CLI、TUI 或动态 broker 中复制第二套 Agent Loop。 | `packages/agent/src/types.ts`；`packages/agent/src/agent-loop.ts`；`packages/agent/test/tool-loop.test.ts` |
| 阶段 2：Plugin runtime 核心 | `partial` | `PluginHost`、`PluginScope`、原子注册、disposer 逆序释放、诊断、prompt、command、middleware、UI contribution、frontend、subagent provider、session projection、动态 Package/ActiveRun 状态机已实现。 | 生产 loader 仍使用 `packages/coding-agent/src/plugins/manifest.ts` 私有 manifest 校验；应统一使用 `@di-code/plugin-runtime` 根入口的校验，删除重复适配并补边界测试。 | **Bounded action A：manifest 校验收口。** 完成 loader 迁移、重复实现清理、成功/非法字段/路径边界/兼容测试、`npm test --workspace @di-code/coding-agent`、`npm run check`、`npm run build`。 | `packages/plugin-runtime/src/index.ts` 的 `parsePluginManifest()`；`packages/coding-agent/src/plugins/manifest.ts`；`packages/coding-agent/src/plugins/loader.ts` |
| 阶段 3：Coding-agent 宿主与 Session 接入 | `complete` | `CodingAgentPluginHost` 已接入 `AgentSession`，支持 prompt、工具、middleware、command、projection、事件和关闭；loader、Session、interactive、CLI 使用新 runtime host；trust、发现和全局插件管理存在。 | 无本阶段已知主缺口。 | 后续改动必须保持 Session、interactive 和 CLI 的单一路径。 | `packages/coding-agent/src/plugins/runtime-host.ts`；`packages/coding-agent/src/core/session.ts`；`packages/coding-agent/src/main.ts`；`packages/coding-agent/test/plugin-system.test.ts` |
| 阶段 4：内置 frontend 与 Controller | `complete` | `InteractiveController` 支持 submit、steer、cancel、retry、command、model、Session、compaction 和结构化事件；builtin frontend 驱动现有 ANSI TUI。 | 无本阶段已知主缺口。 | 不让 frontend 读取 Agent 私有状态、Provider 或 Session JSONL。 | `packages/coding-agent/src/interactive/controller.ts`；`packages/coding-agent/src/interactive/builtin.ts`；`packages/coding-agent/test/interactive-controller.test.ts` |
| 阶段 5：自定义 frontend 与 Profile | `complete` | `--profile terminal/headless`、`--ui`、frontend 注册/选择、核心 capability 校验、终端独占、窄终端 facade、启动/退出清理、profile override、trust 和 pluginIds 筛选已接入真实启动。 | 图片粘贴、树导航、设置页属于可选能力，不是当前完成条件。 | 保持失败启动、取消、退出和非 TTY 清理测试。 | `packages/coding-agent/src/plugins/profile.ts`；`packages/coding-agent/src/startup.ts`；`packages/coding-agent/test/profile.test.ts`；`packages/coding-agent/test/startup.test.ts` |
| 阶段 6：子 Agent | `complete` | `SubagentProvider`、进程内 provider、深度/并发/超时/取消、结果截断、凭据脱敏、父 Session 记录、RPC 生命周期和五个 Agent 工具已实现；终态 run 回收且保留有界结果缓存。 | 无本阶段已知主缺口。 | 后续改动不得让活动 run、监听器或子 Session 在重复关闭后残留。 | `packages/plugin-runtime/src/subagents.ts`；`packages/coding-agent/src/subagents/service.ts`；`packages/coding-agent/test/subagents.test.ts` |
| 阶段 7：旧实现退役与契约固化 | `complete` | 新 API/runtime README、生产 loader、Session、interactive、SDK/CLI fixture 已使用新 runtime；旧 `extensions` 生产实现、入口和独立测试已删除。 | 方案文档中的旧名称只作为明确标注的迁移历史保留。 | 不恢复旧 loader/API；新增插件只使用 `@di-code/plugin-runtime`。 | `packages/coding-agent/src/plugins/loader.ts`；`packages/coding-agent/src/plugins/runtime-host.ts`；`packages/coding-agent/test/sdk-example.test.ts` |
| 上下文 6 / 阶段 8：动态 broker 生命周期 | `complete`（bounded action）；阶段 8 总体仍为 `partial` | `DynamicPluginBroker` 已实现 define/run/update/stop/remove/inspect、显式审批、interactive + `--allow-dynamic-plugins` 门禁、独立 Node 子进程、bootstrap、启动/活动超时、启动前取消、崩溃/零退出/非法协议/输出限制、更新回滚、错误脱敏、重复 stop 和 dispose 资源回收。 | 动态子进程还不能通过协议注册/撤销工具、prompt、middleware 或事件观察器；子进程不是操作系统沙箱。 | **Bounded action C：跨进程能力贡献协议。** 版本化 JSONL 注册/撤销记录；宿主校验能力、命名空间、schema、大小、超时、取消和资源释放；跨进程异常转为脱敏诊断；补正常/失败/重复/取消/回收测试。必须在 Web 上下文之后执行，不能混入 Web 改动。 | `packages/coding-agent/src/plugins/dynamic-broker.ts`；`packages/coding-agent/test/dynamic-broker.test.ts`（9 tests）；`packages/plugin-runtime/src/dynamic/index.ts` |
| 上下文 7 / 阶段 9：Web frontend | `partial` | `WebFrontendHost`、`WebConnection`、`WebClient`、版本化消息、token/frontend/slot 授权、eventId、重连重放、过期事件拒绝、页面断开不销毁 Host 已实现。 | 没有独立 Web app、浏览器 frontend、真实 Web transport 适配和浏览器端 UI。 | **下一 bounded action B：真实 Web frontend。** 实现 transport、浏览器 frontend/app、授权、断线重连、过期事件、页面销毁和 Host 所有权；补浏览器/transport/生命周期测试。完成后阶段 9 才能标记 `complete`。 | `packages/coding-agent/src/web.ts`；`packages/coding-agent/test/web.test.ts`；`packages/coding-agent/src/index.ts` |
| 执行规则（每次 AI 上下文） | `required` | 每次只执行一个 bounded action；先读 `AGENTS.md`、`README.md`、方案文档和本表；先补失败测试，再改实现；运行 owning workspace 测试、`npm run check`，涉及公共入口/CLI/RPC/子进程时运行 `npm run build`。 | 不得根据 Git 提交标题判断完成；不得把测试通过当成未实现功能完成；不得顺带修改其他阶段。 | 完成后必须直接更新本表：当前 action 状态、已实现、未实现、测试实际结果、剩余问题和下一 action。只有代码、测试和本表一致时才能写 `complete`。 | 当前仓库测试门禁：`npm test --workspace @di-code/plugin-runtime`；`npm test --workspace @di-code/coding-agent`；`npm run check`；`npm run build`；`git diff --check` |

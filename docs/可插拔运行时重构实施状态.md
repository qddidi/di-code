# 可插拔运行时重构实施状态

本文件记录每个阶段的可恢复状态。架构目标和公共协议以[可插拔运行时重构方案](./可插拔运行时重构方案.md)为准；本文件只记录当前仓库事实、验证证据和下一阶段上下文。

## Stage 0

### Status

passed

### Changed files

- `packages/coding-agent/test/cli-process.test.ts`：子进程基线改为执行 `src/entry.ts`，使用 Node `--experimental-strip-types`，不依赖生成的 `dist`。
- `packages/coding-agent/test/runtime-baseline.test.ts`：新增源码 RPC 入口的 Faux `get_state`、`prompt`、`cancel` 基线测试。
- `docs/可插拔运行时重构实施状态.md`：新增本状态与硬编码迁移清单。

未修改生产路径、公共运行时、workspace 依赖、`dist`、coverage、`.di-code`、`.env`、会话或凭据。开始审计时 `git status --short` 仅显示分支 `plugin2`，没有用户已有未提交改动；因此没有重叠文件需要保留或解决。

### Public API

没有新增或修改公共 API。当前公共边界仍为：

- `@di-code/ai`：Provider-neutral `Provider`、`Model`、消息、工具 schema 和流事件；内建 OpenAI、Anthropic、DeepSeek、Kimi、Zhipu、Faux adapter 仍由该包导出。
- `@di-code/agent`：`Agent` 和唯一的 model-tool loop；coding-agent 负责工具实现、Session 和产品模式。
- `@di-code/coding-agent`：CLI `entry`、`AgentSession`、Session JSONL、`rpc` 子路径和旧插件/扩展 API。
- `@di-code/mcp`：MCP client/manager 生命周期、分页、取消和错误分类；不实现 Agent loop。
- `@di-code/orchestrator`：只通过 `@di-code/coding-agent/rpc` 公开协议监督子进程。
- `@di-code/tui`：presentation-only 的 ANSI terminal、TUI 和组件。

RPC 仍固定 `version: 1`、请求 `id`、事件 `requestId`，方法为 `prompt`、`cancel`、`get_state`。JSON mode 的事件版本仍为 `2`。

### Composition entries

阶段 0 只建立迁移目标，不接线新 Runtime。下表的“当前直接构造/依赖”来自源码和测试；“目标 scope / Registry / plugin entry”对应方案中的目标 Context 层次。

| 当前构造点 | 当前直接依赖和用户可见行为 | 目标 Context scope | 目标 Registry | 目标 plugin entry |
| --- | --- | --- | --- | --- |
| `packages/coding-agent/src/entry.ts` | 解析启动配置、TTY onboarding/trust、调用 `runMain`；`di-code` help/version、print/json/interactive 启动 | Root | `HostCommandRegistry`, `ModeRegistry` | `bootstrap`, `cli-parser`, `mode-*`, `exit` |
| `packages/coding-agent/src/main.ts` | 直接导入 `AgentSession`、`SessionManager`、资源/system prompt、MCP、plugins、TUI 和三种 mode；选择 Provider runtime；输出 plugin/MCP diagnostics | Root -> Project -> Session | `ProviderRegistry`, `SessionFactoryRegistry`, `PromptSectionRegistry`, `ModeRegistry`, `CommandRegistry` | `runtime-bootstrap`, `provider-*`, `agent-session`, `session-store-jsonl`, `resource-loader`, `system-prompt`, `mcp-*`, `mode-*`, `tui-renderer` |
| `packages/coding-agent/src/rpc-entry.ts` | 直接 `new AgentSession`、`new RpcServer`；stdin/stdout JSONL RPC；stderr 错误 | Root -> Session | `SessionFactoryRegistry`, `RpcMethodRegistry` | `rpc-protocol-v1`, `rpc-server`, `rpc-events`, `rpc-client-sdk` |
| `AgentSession` (`src/core/session.ts`) | 创建 read/write/edit/bash/glob/grep/load_skill 七类工具，接收 `ExtensionHost` 和 MCP external tools；管理 Agent loop、Provider、Session append、branch、compaction、usage | Session -> Agent -> Turn -> ToolCall | `ToolRegistry`, `ProviderRegistry`, `SessionStoreRegistry`, `CompactionRegistry`, `ResourceRegistry` | `agent-loop`, `agent-session`, `tool-*`, `session-store-jsonl`, `session-tree`, `usage-meter`, `context-budget`, `compaction-basic`, `skills`, `resource-loader` |
| `src/startup.ts` / `@di-code/ai` providers | 环境/settings precedence、模型选择、missing key/unknown model；Faux 离线 | Project / Request | `ProviderRegistry`, `ModelRegistry`, `CredentialEnvRegistry` | `provider-openai`, `provider-anthropic`, `provider-deepseek`, `provider-kimi`, `provider-zhipu`, `provider-faux`, `model-catalog`, `runtime-selection` |
| `src/core/tools/*` | 文件读写编辑、搜索、shell；root/path/symlink、timeout/output/cancel 和 schema 校验 | ToolCall | `ToolRegistry`, capability services | `workspace`, `process`, `tool-read`, `tool-write`, `tool-edit`, `tool-bash`, `tool-glob`, `tool-grep`, `tool-load-skill`, `approval`, `policy`, `tool-output` |
| `src/core/session/*` | JSONL v2 append-only、lock、坏记录 diagnostics、restore、tree/branch、summary | Session | `SessionStoreRegistry`, `SessionQueryRegistry` | `session-store-jsonl`, `session-tree`, `session-query`, `context-budget`, `compaction-basic` |
| `src/extensions/*` + `src/plugins/*` | 旧 `ExtensionHost` 保存 tools/commands/handlers；manifest loader、project trust、diagnostic、manager enable/disable/install/remove | Project / Session | 目标统一 `Registry` + `EventBus` + `PluginInventory` | `plugin-loader`, `plugin-sdk`, `plugin-manager`, `trust`, `diagnostics` |
| `src/mcp/*` | project trust 后加载 stdio/HTTP MCP；tools/list、resources/prompts、工具适配、连接失败隔离、close | Project -> Session -> ToolCall | `ToolRegistry`, `McpServerRegistry` | `mcp-config`, `mcp-client`, `mcp-tools` |
| `src/modes/interactive.ts` + `@di-code/tui` | 虚拟/真实终端布局、ANSI、slash command、cancel/retry/theme/session choices；TUI 只渲染 | Session / Request（UI host 在 Root） | `ModeRegistry`, `CommandRegistry`, `ThemeRegistry`, `RendererRegistry` | `mode-interactive`, `tui-renderer`, `theme`, `command-core`, `command-session`, `command-model`, `command-settings` |
| `packages/orchestrator/src/supervisor.ts` | 通过公开 RPC SDK 启停子进程、握手、prompt/cancel、崩溃和 stderr 截断 | Root（外部进程） | 公开 RPC SDK，不访问内部 Registry | `orchestrator-host` |

### Tests / Checks

实际运行并通过：

- `npm run check`：Biome 和 strict TypeScript 全部通过。
- `npm test`：所有 workspace 通过；coding-agent `34` 个测试文件、`388` 项通过；ai `156` 项通过、`6` 个真实 Provider smoke 项按无凭据规则跳过；agent `53`、mcp `5`、orchestrator `8`、skills `8`、tui `186` 项通过。
- `git diff --check`：通过。
- `npm test --workspace @di-code/coding-agent -- --run test/cli-process.test.ts test/runtime-baseline.test.ts`：新增/调整的源码入口基线 `8/8` 通过。

覆盖矩阵：

- Faux print/json：`test/cli-process.test.ts`、`test/print.test.ts`、`test/json.test.ts`；源码入口另由 `runtime-baseline.test.ts` 覆盖 RPC。
- Interactive 虚拟终端：`test/interactive.test.ts` 使用 `TestTerminal` 覆盖启动回滚、80x24/窄终端布局、输入、取消、重试、Session 切换；`@di-code/tui` 使用 `VirtualTerminal`/`EmulatedTerminal` 覆盖 ANSI 和布局。
- RPC prompt/cancel/get_state：`test/rpc-client-server.test.ts`、`test/rpc-process.test.ts`，以及新增源码子进程基线。
- Session restore/branch：`test/session.test.ts`、`test/session-manager.test.ts` 覆盖 v2 restore、append 顺序、锁、坏记录、树和 sibling branch。
- 工具安全边界：`read-tool.test.ts`、`write-tool.test.ts`、`edit-tool.test.ts`、`bash-tool.test.ts` 覆盖路径、symlink、schema、shell、超时、输出上限和 AbortSignal。
- 插件 trust/diagnostic：`extensions-runtime.test.ts`、`plugin-system.test.ts` 覆盖 trust 跳过、manifest/路径/权限校验、加载失败、冲突回滚、handler 隔离和凭据模式脱敏。

### Lifecycle/Security evidence

- 当前 Agent loop 的唯一执行者仍是 `@di-code/agent`；`AgentSession` 只组装工具并转发事件，RPC server 不实现第二套 loop。
- Session JSONL v2 追加、跨 manager lock、恢复诊断、branch context 和 compaction 语义已有测试；失败 append 不污染内存或后续队列。
- 文件工具使用 allowed-root canonicalization 并拒绝 parent traversal、外部绝对路径、外部/dangling symlink；bash 使用结构化 spawn 参数、明确 cwd、超时、取消和独立输出上限。
- 插件是同进程代码，不是沙箱；project trust 仅决定是否加载项目代码。manifest ID、entry root、权限声明、工具 namespace 冲突均校验，handler 诊断会脱敏 `token` 等凭据模式。
- MCP URL、headers、stdio command/args 由 MCP 包和宿主配置校验；连接失败只禁用对应 Server，并在结束时 close。
- RPC JSONL 校验版本、id/requestId、参数和事件；client 在子进程退出时拒绝 pending 请求，server 对并发 prompt 返回 `BUSY`，cancel 通过 AbortSignal 传播。
- TUI 测试覆盖 terminal startup failure 的状态回滚和 stop 幂等；orchestrator 只依赖公开 RPC SDK。

### Known risks

- 新 Runtime 尚未实现；上述目标 Registry/scope/entry 是迁移清单，不是当前可用 API。
- `main.ts`、`rpc-entry.ts`、`AgentSession` 仍直接 import/construct 生产实现，旧 `plugins`/`extensions` 双轨系统仍存在；这是后续阶段的主要耦合风险。
- coding-agent 的子进程测试必须执行源码入口或先构建；本阶段已消除对未提交 `dist` 的测试依赖，但发布产物仍需在后续 build/release gate 验证。
- 真实 Provider 网络行为未在无 key 环境验证；仅离线 Faux 和协议/适配器单测作为基线。
- RPC server 当前仍由 `rpc-entry.ts` 直接创建 Session；shutdown flush/dispose 顺序和 Registry method namespace 属于 Stage 12 风险。

### Next stage context

Stage 1 应只新增 `@di-code/plugin-runtime`、`@di-code/plugin-loader`、`@di-code/plugin-sdk` 的 workspace 和公共契约骨架：`PluginDefinition`、`Context`、`Fiber`、`ServiceKey`、`Disposer`、`PluginStatus`、`RuntimeEvent`、`RuntimeMode`、`ConfigSchema`、`PluginCapabilities`、Registry 基础类型及 namespace fixture。不得迁移 `main`、`AgentSession`、Provider、工具、Session、MCP、TUI 或 RPC 生产路径；继续保持本阶段基线测试绿色。

## Stage 1

### Status

passed

### Changed files

- `packages/plugin-runtime/`：新增独立 workspace、根入口契约、README 和 discriminant/guard 测试。
- `packages/plugin-loader/`：新增仅依赖 `@di-code/plugin-runtime` 的 namespace export 校验、README、测试和无 `default` 的真实 fixture。
- `packages/plugin-sdk/`：新增只从两个公开根入口 re-export 的 SDK、README 和根入口测试。
- `package.json`：将三个 workspace 按 runtime -> loader -> SDK 顺序加入根 build 脚本。
- `package-lock.json`：登记三个 workspace link 及固定 `0.1.7` 内部依赖。
- `tsconfig.json`：为根 strict 检查增加三个新包的源码路径映射，避免历史生成 `dist` 遮蔽公共源码契约。
- `docs/可插拔运行时重构实施状态.md`：记录本阶段证据和下一阶段上下文。

用户已有的 `docs/可插拔运行时重构VibeCoding实施步骤验证.md` 改动未修改；它与本阶段文件不重叠。未编辑 `dist`、coverage、`.di-code`、`.env`、会话或凭据。

### Public API

- `@di-code/plugin-runtime` 根入口导出 `PluginDefinition`、`Context`、`Fiber`、`ServiceKey`、`Disposer`、`PluginStatus`、`RuntimeEvent`、`RuntimeMode`、`ConfigSchema`、`PluginCapabilities` 以及 `Registry`、`RegistryEntry`、`RegistryOwner`、`RegistrySnapshot` 基础类型。
- 同一入口导出 `createServiceKey`、`isPluginStatus`、`isRuntimeMode` 和 `isRuntimeEvent` 守卫。
- `@di-code/plugin-loader` 根入口导出 `PluginModule`、`isPluginDefinition` 和 `getPluginDefinition`；namespace 模块必须有非空 `name` 与 `apply`，有 `default` 时拒绝。
- `@di-code/plugin-sdk` 只从上述两个公开根入口 re-export，未使用 deep import。

### Composition entries

本阶段只登记公共组合边界，不迁移产品实现：`plugin-runtime`（基础 Context/Fiber/Registry 契约）、`plugin-loader`（namespace definition boundary）、`plugin-sdk`（外部插件公开入口）。`main`、`AgentSession`、Provider、工具、Session、MCP、TUI 和 RPC 仍保持 Stage 0 的直接构造路径。

### Tests/Checks

实际运行并通过：

- `npm test --workspace @di-code/plugin-runtime`：3/3。
- `npm test --workspace @di-code/plugin-loader`：3/3，包含真实 namespace fixture 和 default export 拒绝。
- `npm test --workspace @di-code/plugin-sdk`：1/1，验证 SDK 根入口 re-export。
- `npm run check`：Biome 与 strict TypeScript 通过。
- `npm run build --workspace @di-code/plugin-runtime`、`@di-code/plugin-loader`、`@di-code/plugin-sdk`：通过。

根 `npm run build` 与 `git diff --check` 在本状态记录更新后执行并作为最终门禁证据补充。

### Lifecycle/Security evidence

- `PluginDefinition.apply` 明确接收 `Context`、配置和 owner `Fiber`，并允许返回 disposer；Registry 基础条目携带 `RegistryOwner`，为后续自动撤销提供契约位置。
- `Context` 暴露 `AbortSignal`、父子关系和 isolate 选项；本阶段只定义类型，不宣称尚未实现的卸载行为。
- loader 在模块边界拒绝 `default` export 和不完整 definition；SDK 不暴露私有路径。
- `PluginCapabilities` 仅是声明/审计元数据，in-process plugin 仍不是 Node 沙箱；trust、能力执行门禁、加载/卸载和迟到注册检查留待后续阶段。

### Known risks

- `packages/plugin-runtime/dist` 中存在 Stage 0 之前的历史生成物；本阶段未编辑它，根 `tsconfig` 的 paths 只用于源码检查，发布构建仍由各 workspace 生成新的 dist。
- Context/Fiber/Registry 目前是公共契约骨架，没有真实运行时实现、依赖拓扑、回滚、逆序 dispose、事件总线或 composition inventory。
- 旧 `plugins`/`extensions` 与 `ExtensionHost` 仍存在，产品代码尚未迁移；不能据此宣称完全可插拔。
- workspace 版本固定为当前 `0.1.7`，后续版本准备脚本需要把三个 public package 纳入版本同步/发布检查。

### Next stage context

Stage 2 应实现 runtime 的最小行为（Root/child Context、isolate 可见性、Fiber 状态和幂等逆序 disposer、owner-aware Registry、RuntimeEvent 发布），并用失败回滚、取消、重复 dispose 和 Session 隔离测试锁定语义。不得在 Stage 2 提前迁移 `coding-agent` 产品构造；继续保持 loader 只依赖 runtime、SDK 只依赖公开根入口。

## Stage 2

### Status

passed

### Changed files

- `packages/plugin-runtime/src/index.ts`：实现 Root/Child Context、isolate 服务可见性、`ServiceRegistry` owner 记录、RuntimeEventBus、Fiber 状态机和 disposer。
- `packages/plugin-runtime/test/runtime.test.ts`：新增 pending async setup、apply 回滚、Context isolate、duplicate service、AbortSignal、迟到 callback、逆序/聚合/幂等 dispose 行为测试。
- `packages/plugin-runtime/README.md`：同步 Stage 2 生命周期、服务发布和隔离语义。
- `docs/可插拔运行时重构实施状态.md`：记录 Stage 2 证据和下一阶段上下文。

保留用户已有的 `.playwright-mcp/page-2026-08-19T09-07-18-653Z.yml` 删除改动；该文件与本阶段不重叠。未编辑 `dist`、coverage、`.di-code`、`.env`、会话或凭据，也未连接 CLI、Agent、Provider 或文件工具。

### Public API

- `createRootContext()`、`createChildContext()`、`RootContext`、`ChildContext`。
- `Context.get/require/set/child/plugin/dispose`，以及 `Context.signal`、`Context.events` 和 `Context.services`。
- `RuntimeFiber`、`createFiber()`、`activatePlugin()`；Fiber 状态为 `pending -> loading -> active`，失败为 `failed`，卸载为 `unloading -> disposed`。
- `ServiceRegistry`、`ServiceRegistryEntry`；pending entry 不可见，commit 后记录保留 owner Fiber、scope/context ID，重复 key 在同一 registry 明确报错。
- `RuntimeEventBus`；订阅 disposer 幂等，observer 异常隔离，不改变生命周期状态。

### Composition entries

本阶段只实现 runtime 内部基础组合：`RootContext`、普通/isolated `ChildContext`、fake `PluginDefinition.apply`、owner-aware `ServiceRegistry` 和 RuntimeEventBus。没有修改 `plugin-loader`、`plugin-sdk`、`coding-agent`、`agent`、Provider、Session、MCP、TUI、RPC 或 orchestrator 生产路径。

### Tests/Checks

实际运行并通过：

- `npm test --workspace @di-code/plugin-runtime`：2 个测试文件、10 项通过。
- `npm run check`：Biome 与 strict TypeScript 通过。
- `npm run build --workspace @di-code/plugin-runtime`：通过。
- `git diff --check`：通过。

行为证据覆盖：服务在异步 apply 完成前不发布；apply 失败删除全部服务；普通子 Context 继承且 isolate 子 Context 不泄漏；同 scope duplicate 明确报错；dispose abort pending setup、拒绝迟到 callback、逆序执行、聚合多个错误并可重复调用。

### Lifecycle/Security evidence

- 服务注册以 Fiber 为 owner；Fiber 进入 `active` 后才 commit，失败或卸载 rollback。迟到 callback 经过 Fiber 状态检查，不能在 `unloading`/`disposed` 中追加服务。
- Context dispose 先 abort 自身 signal，再逆序 dispose 子 Context 和 Fiber；Fiber dispose 等待 in-flight apply，之后逆序运行 disposer。重复 dispose 复用同一 promise。
- isolate Context 使用独立 registry，不能读取父 scope 的服务；普通子 Context 只通过父 Context 查询继承，不复制 registry。
- RuntimeEventBus 只用于观察和诊断，handler 错误隔离；插件仍是同进程代码，能力字段不是 Node 沙箱。

### Known risks

- `ServiceRegistry` 当前是最小 typed service registry，尚未提供 Loader 所需的依赖拓扑、optional injection、composition inventory 或跨 package registry 类型适配。
- `Context.plugin()` 当前只负责单个 fake definition 的激活，不负责 namespace/manifest、trust、capability gate 或动态 import；这些属于后续 Loader/SDK 阶段。
- 旧 `plugins`/`extensions`、`ExtensionHost` 和产品直接构造路径仍存在，不能据此宣称完全可插拔。
- 真实 Provider 网络行为和 CLI/RPC/TUI 组合未在本阶段验证。

### Next stage context

Stage 3 应在不迁移产品路径的前提下实现 `plugin-loader` 的 Fiber/Context 激活组合：依赖检查、required/optional failure、entry tree 和 inventory；继续复用本阶段的 owner rollback、AbortSignal、迟到注册拒绝、isolate scope 和 disposer 语义。不得编辑 `dist` 或提前连接 CLI、Agent、Provider、文件工具、Session、MCP、TUI、RPC。

## Stage 3

### Status

passed

### Changed files

- `packages/plugin-runtime/src/index.ts`：新增 typed asynchronous `EventBus<E>`、`DiagnosticSink`、`PluginLogger`、凭据脱敏、`CapabilityView` 和 fake capability view；将 capability/logger 视图注入 Context/Fiber。
- `packages/plugin-runtime/test/stage3.test.ts`：新增排序、observer 错误隔离、critical gate、timeout/abort、自动 unsubscribe、dispose 负例、trust/声明 capability 负例和诊断脱敏测试。
- `packages/plugin-runtime/README.md`：记录 Stage 3 API、失败和安全边界。
- `docs/可插拔运行时重构实施状态.md`：记录本阶段证据和下一阶段上下文。

### Public API

- `EventBus<E>`、`EventHandler`、`EventHandlerOptions`、`EventDispatchResult`；支持 priority、稳定排序、critical gate、handler timeout、AbortSignal、自动 unsubscribe 和幂等 dispose。
- `DiagnosticSink`、`DiagnosticRecord`、`PluginLogger`、`createDiagnosticSink`、`createPluginLogger`、`redactSensitiveText`、`redactError`。
- `CapabilityView`、`CapabilityName`、`CapabilityDeniedError`、`createCapabilityView`、`createFakeCapabilityView`。

### Composition entries

本阶段只在 `plugin-runtime` 内组合 EventBus、诊断和 capability view；Root/Child Context 将 trust 作为策略输入，Plugin Fiber 从 definition 的声明生成 capability view。未迁移 CLI、Agent、Provider、工具、Session、MCP、TUI、RPC 或 orchestrator。

### Tests/Checks

实际运行并通过：

- `npm test --workspace @di-code/plugin-runtime`：3 个测试文件、16 项通过。
- `npm run check`：Biome 与 strict TypeScript 通过。
- `npm run build --workspace @di-code/plugin-runtime`：通过。
- `git diff --check`：通过。

### Lifecycle/Security evidence

- EventBus 订阅 disposer 幂等；订阅 signal 自动移除；emit 在 priority/sequence 顺序串行执行，observer 错误隔离，critical 错误停止后续 handler；timeout 和 caller/handler AbortSignal 都会结束等待。
- DiagnosticSink 在交付前脱敏 `token`、`secret`、`authorization`、`api_key`，handler 错误通过 sink 记录而不泄露原始消息。
- CapabilityView 只有在 project trusted 且 capability 由插件声明时才允许 `require`；fake implementation 只返回测试值，不暴露 Node `fs`、`spawn` 或 `process`。
- Context/Fiber dispose 后 EventBus 拒绝 emit/注册，Context/Fiber 的服务注册仍由 owner 生命周期回滚。

### Known risks

- EventBus 当前是 runtime 内部公共 primitive，尚未接入旧 `plugins`/`extensions` 或产品事件流；critical gate 的业务语义由后续 composition owner 定义。
- capability 目前是接口和 fake implementation，尚未绑定真实文件、网络或进程服务；in-process plugin 仍不是 Node 沙箱。
- 全仓 build/release 与真实 Provider 网络行为未在本阶段验证。

### Next stage context

Stage 4 应在 `plugin-loader` 中组合依赖检查、required/optional failure、entry tree 和 inventory，并复用 Stage 3 的 EventBus 诊断、capability/trust gate 和 Fiber 回滚语义；不得提前迁移 coding-agent 产品构造路径。

## Stage 4

### Status

passed

### Changed files

- `packages/plugin-runtime/src/contribution-registry.ts`：统一 ContributionRegistry、十类贡献最小公共类型及边界校验。
- `packages/plugin-runtime/src/index.ts`：导出 Stage 4 公共 API。
- `packages/plugin-runtime/test/contribution-registry.test.ts`：纯 registry 行为、快照和验证测试。
- `docs/可插拔运行时重构实施状态.md`：记录本阶段证据和下一阶段上下文。

### Public API

- `ContributionRegistry`、`ContributionRegistryError`、`ContributionKind`。
- Provider、Tool、Command、Prompt、SessionStore、SessionFactory、Compaction、Renderer、RpcMethod、Resource 最小贡献类型。
- `register` 返回 owner disposer；`list` 与 `snapshot` 返回只读、冻结视图。
- `is/assertToolSchema`、`is/assertProviderModel`、`is/assertRpcMethodContribution` 运行时校验。

### Composition entries

本阶段只建立统一 registry 与公共契约，未迁移现有 Provider、工具、Command、Prompt、SessionStore、AgentSession、Compaction、Renderer、RPC 或 Resource 实现。

### Tests/Checks

实际运行并通过：

- `npm test --workspace @di-code/plugin-runtime`：4 个测试文件、20 项通过。
- `npm run check`：Biome 与 strict TypeScript 通过。
- `npm run build --workspace @di-code/plugin-runtime`：通过。
- `git diff --check`：通过。

### Lifecycle/Security evidence

- 注册记录保存 owner；disposer 幂等删除且只删除自身记录。
- duplicate、reserved、namespace conflict 在注册时失败；快照按 kind 和限定名稳定排序并冻结。
- Tool schema、RPC params/method name、Provider model 在边界处运行时校验。
- 未引入 shell、eval、凭据读取或 AgentSession 改动；registry 不构成插件沙箱。

### Known risks

- 贡献实现尚未接入产品组合流程；本阶段只冻结契约和 registry 行为。
- JSON Schema 校验保持最小对象 schema 检查，完整参数语义仍由调用方负责。

### Next stage context

下一阶段可在不改变 `ContributionRegistry` 公共行为的前提下，逐步接入 Provider、Tool、Command 与 Prompt composition，并为每个接入点补充 owner 生命周期和安全边界测试。

## Stage 5

### Status

blocked

### Changed files

- `packages/plugin-loader/src/index.ts`：新增 JSON/YAML composition parser/reader、受限环境变量解析、固定层合并、Entry Tree/inventory、patch 操作、依赖拓扑排序和基于 Context/Fiber 的真实 Loader。
- `packages/plugin-loader/package.json`、`package-lock.json`：加入 `yaml` 解析依赖。
- `packages/plugin-loader/test/loader.test.ts`：覆盖 namespace fixture、层合并、patch、配置表达式、依赖缺失/循环、disabled/optional/required failure 和真实动态 Loader 入口。
- `packages/plugin-loader/test/fixtures/composition-plugin/`、`composition-required.ts`、`composition-optional.ts`：fixture plugin package 与失败边界 fixture。
- `packages/plugin-loader/README.md`：记录 composition、inventory、失败和安全语义。
- `docs/可插拔运行时重构实施状态.md`：记录 Stage 5 证据和下一阶段上下文。

未修改 `dist`、coverage、`.di-code`、`.env`、会话或凭据。开始本阶段时 `git status --short` 为空，没有与本阶段重叠的用户已有改动；本阶段工作区新增/修改文件如上。

### Public API

- `CompositionEntry`、`CompositionDocument`、`CompositionLayer`、`CompositionPatch`、`JsonValue`。
- `parseComposition()`、`readCompositionFile()`、`mergeCompositionLayers()`、`applyCompositionPatch()`、`topologicallySortEntries()`。
- `EntryTree`、`EntryRecord`、`PluginInventory`、`CompositionLoader`、`createCompositionLoader()`、`LoaderOptions`。
- `CompositionError` 使用 `invalid`、`duplicate`、`patch`、`missing-dependency`、`cycle`、`required-failure` discriminants。

### Composition entries

支持固定顺序 `base -> mode -> user -> project -> explicit`（由调用方按该顺序传入 layers）；后层可覆盖同 id entry，并通过明确 id 执行 `insert/append/remove/replace/enable/disable/move`。Entry id 唯一，`group` 仅为树组织元数据；`dependsOn` 为 required 依赖，`optionalDependsOn` 缺失时不阻止激活。Loader 依拓扑顺序稳定激活，disabled entry 在 import 前跳过，inventory 显示 pending/loading/active/failed/disabled/skipped。

### Tests/Checks

实际运行并通过：

- `npm test --workspace @di-code/plugin-loader`：8/8。
- `npm run check`：Biome 与 strict TypeScript 通过。
- `npm run build --workspace @di-code/plugin-loader`：通过。
- `npm install --package-lock-only --ignore-scripts`：lockfile 同步通过。
- `git diff --check`：作为最终门禁执行。
- `npm run build`：通过。
- `npm test`：未通过；仅 `packages/coding-agent/test/bash-tool.test.ts` 的 3 个 Windows timeout/EBUSY 用例失败（其余 workspace，包括 plugin-loader 8/8，通过）。

### Lifecycle/Security evidence

- Loader 只通过 `Context.plugin()` 激活 definition，Fiber owner 负责 apply 失败回滚、逆序 dispose 和重复关闭；required failure 会 dispose 已激活 sibling，optional failure 只写入 skipped/error inventory。
- required/optional dependency 缺失、循环和非 active 依赖均有界报错；同优先级不依赖的 entry 保持 composition 顺序，拓扑排序稳定。
- composition parser 只接受 JSON/YAML 数据；环境变量仅接受完整 `$NAME`/`${NAME}`，缺失变量显示变量名；拒绝命令替换、反引号、非有限数和非法模块表达式。
- disabled entry 不调用 importer；真实 fixture 测试通过 `CompositionLoader` 动态 import 入口验证，而非直接调用 `plugin.apply`。插件仍是 in-process 代码，runtime trust/capability 不是沙箱。

### Known risks

- Loader 当前 API 已独立可用，但尚未接入 coding-agent `main`、CLI、RPC、Provider、Agent、Session、MCP 或 TUI；产品默认 composition 仍待后续阶段迁移。
- `group` 目前保留为 Entry Tree 元数据，尚未提供按 group 的启停命令或 include 语法；显式 composition layer 路径解析由宿主负责。
- package manifest `diCode`/api major/export boundary 和安装管理仍未迁移；本阶段 fixture 使用本地真实 namespace module。
- 全仓 `npm test` 的 coding-agent bash-tool Windows timeout/EBUSY 失败阻止本阶段完成门；需要修复或稳定该既有测试环境问题后才能改为 `passed`。release dry-run 未运行。

### Next stage context

Stage 6 可在保持 Loader 公共 API 的前提下接入 plugin package manifest/apiVersion/export 边界、trust/capability import gate 和 profile composition 文件发现；随后再迁移 Provider/Agent/Session 的最小 faux composition。不得编辑 `dist` 或绕过 Loader 直接构造产品实现。

## Stage 6

### Status

passed

### Changed files

- `packages/plugin-loader/src/index.ts`：加入 manifest `apiVersion`、unsafe id、entry/path、capability 和 package `exports` 校验；namespace export unwrap/default 拒绝；project-local trust gate；版本化 `ProjectTrustStore`、`PluginRegistry` 与 `PluginInstallManager`，支持 local/npm/git staging、npm `--ignore-scripts`、managed path `resolve+relative`、原子 registry 写入和失败回滚。
- `packages/plugin-loader/test/loader.test.ts`：新增 published namespace、malformed manifest、missing export、import failure、disabled、untrusted project、trust persistence 和 install rollback 真实行为测试；共 17 项通过。
- `packages/plugin-loader/test/fixtures/published-namespace/`：公开 package `exports` namespace plugin fixture。
- `packages/plugin-loader/test/fixtures/missing-export/`、`import-failure.ts`、`malformed-manifest.json`：manifest/export/import 失败 fixture。
- `packages/plugin-loader/test/fixtures/composition-plugin/package.json`：补充公开 entry export 声明。
- `packages/plugin-loader/README.md`：同步 Stage 6 manifest、exports、trust 和安装/回滚边界。

开始本阶段时 `git status --short` 为空，最近相关 commit 为 `2450ad9 feat: Composition Parser 和 Loader`；没有与用户已有改动重叠的文件。未编辑 `dist`、coverage、`.di-code`、`.env`、会话或凭据，也未自动 commit/push/reset/checkout/delete。

### Public API

- `PluginCapability`、`PluginCapabilities`、`PluginManifest`、`parsePluginManifest()`、`readPackagePluginManifest()`。
- `resolvePackagePluginExport()`、`resolvePluginEntry()`、`unwrapPluginModule()`、`getPluginDefinition()`；发布 namespace 必须有 `name`/`apply`，可选 `apiVersion` 必须为 `1`，拒绝 `default`。
- `ProjectTrustStore`、`ProjectTrustRecord`；trust JSON 固定 `version: 1`，按 canonical project path 记录决策。
- `ManagedPlugin`、`PluginRegistry`、`PluginInstallManager`、`assertManagedPath()`；registry 固定 `version: 1`，安装支持 local/npm/git staging、enable/disable/remove。

### Composition entries

`CompositionEntry.projectLocal` 受 `LoaderOptions.projectTrusted` 控制：未信任时在 import 前记录 `skipped`。disabled entry 同样不 import；required import/definition failure 触发已有 Fiber 逆序 rollback，optional failure 留在 inventory 诊断中。包插件只接受 `diCode.plugins` 中恰好一个、且在 `package.json.exports` 中声明的入口。

### Tests/Checks

实际运行并通过：

- `npm test --workspace @di-code/plugin-loader`：17/17。
- `npm test --workspace @di-code/plugin-sdk`：1/1。
- `npm run check`：Biome 与 strict TypeScript 通过。
- `npm run build --workspace @di-code/plugin-loader`：通过。
- `npm test`：全部 workspace 通过（388 coding-agent tests；plugin-loader 16；总执行无失败）。
- `npm run release:dry-run`：通过，7 packages，outside-install 和 RPC smoke 通过。
- `git diff --check`：通过。

### Lifecycle/Security evidence

- Manifest、capability、apiVersion 和 package export target 在 import 前验证；`resolve+relative` 加 `realpath` 拒绝绝对路径、`..`、空目标和 symlink 逃逸。
- Project trust 只决定 project-local import eligibility，不是 Node 沙箱；capability 仍是声明/审计与 runtime policy 输入。
- npm 安装固定 `npm install --ignore-scripts`；git 使用 depth-1 clone；local/npm/git 都先进入 staging，managed destination 经过边界校验，registry 使用临时文件 + rename，registry 失败恢复旧目录并清理 staging。
- disabled/untrusted entries 不调用 importer；required failure 通过 CompositionLoader 复用 Fiber rollback；诊断保留来源/entry 语义且不记录凭据。

### Known risks

- `PluginInstallManager` 当前是 loader 公共安装 primitive，尚未接入 coding-agent CLI/CommandRegistry；旧 `coding-agent/src/plugins` 双轨系统仍保留，下一阶段才迁移产品入口。
- registry 并发写入尚未提供跨进程锁；原子 rename 防止半写入，但多进程同时更新仍是后续 manager 阶段风险。
- npm specifier 解析覆盖常用 unscoped/scoped package 形式；复杂 alias/workspace specifier 仍需后续明确策略。
- in-process plugin 仍拥有 Node 进程权限，真实不可信代码必须使用独立进程 bridge。

### Next stage context

Stage 7 应在不依赖旧 `coding-agent/src/plugins` 的前提下建立最小 faux profile（runtime + provider-faux + agent + memory + print），并将 manifest/trust/loader 作为 Bootstrap composition 入口；继续保持 package exports 只通过公开根入口，补充 profile fixture 的 mount/unmount 和 CLI smoke。

## Stage 7

### Status

passed

### Changed files

- `packages/builtins/src/index.ts`：新增 Bootstrap、runtime、diagnostics、process-exit、agent-loop、session-memory、mode-print 服务和 owner-aware disposer；保留 provider-registry/provider-faux，并导出最小 profile entry tree。
- `packages/builtins/src/{bootstrap,runtime,diagnostics,process-exit,provider-registry,provider-faux,agent-loop,session-memory,mode-print}.ts`：公开 namespace plugin entry，符合 Loader 的 `name`/`version`/`apply` 边界。
- `packages/builtins/package.json`、`packages/builtins/README.md`：补齐 entry exports、Agent 依赖和 profile 说明。
- `packages/coding-agent/src/main.ts`：新增仅由 RootContext、CompositionLoader 和 HostCommandRegistry 驱动的 minimal profile；不导入旧产品路径。
- `packages/coding-agent/src/legacy-main.ts`：保留 Stage 6 产品路径作为 baseline 参照，未被 minimal composition 导入执行。
- `packages/coding-agent/src/entry.ts`：源码 CLI 只启动 minimal profile，并支持受控 `DI_CODE_TRACE_PLUGINS=1` 生命周期诊断。
- `packages/coding-agent/test/minimal-profile-process.test.ts`：新增真实子进程 Faux print、stdout/退出码、plugin_status、Session dispose 和无 required failure 断言。
- `packages/coding-agent/test/cli-process.test.ts`：同步 Stage 7 最小 profile 的 `--print`/provider 失败行为。
- `tsconfig.json`、`packages/coding-agent/package.json`、`package-lock.json`：登记 builtins、plugin-loader、plugin-runtime 公开 workspace 边界。
- `README.md`：说明 Stage 7 minimal profile 限制和后续迁移范围。

未编辑 `dist`、coverage、`.di-code`、`.env`、会话或凭据；未自动 commit、push、reset、checkout 或删除用户文件。开始阶段时 `git status --short` 和当前 diff 为空，没有用户已有重叠文件。

### Public API

- `@di-code/builtins` 根入口导出 `minimalProfile`、provider/session service keys、Bootstrap/Agent/print service contracts。
- `@di-code/builtins/<entry>` 导出标准 namespace plugin：`apiVersion`、`name`、`version`、`apply`。
- `@di-code/coding-agent` 源入口新增内部 `runMinimalProfile()`；CLI public behavior for this profile is `DI_CODE_PROVIDER=faux npm run dev -- --print "..."`。
- Existing tests that cover the Stage 6 product baseline import the explicit `legacy-main.ts`; no new profile entry imports `loadPlugins` or `loadExtensions`.

### Composition entries

最小 profile 固定按 Loader 拓扑挂载：`Bootstrap`、`runtime`、`diagnostics`、`process-exit`、`provider-registry`、`provider-faux`、`session-memory`、`agent-loop`、`mode-print`。`mode-print` 仅注册 `HostCommandRegistry.print`；Agent loop 使用 `@di-code/agent` 唯一循环，provider 选择来自 `ProviderRegistry`。

### Tests/Checks

实际运行并通过：

- `npm run check`。
- `npm run build --workspace @di-code/builtins`。
- `npm run build --workspace @di-code/coding-agent`。
- `npm test --workspace @di-code/plugin-loader`：17/17。
- `npm test --workspace @di-code/plugin-runtime`：20/20。
- `npm test --workspace @di-code/agent`：53/53。
- `npm test --workspace @di-code/coding-agent`：389/389。
- `npm test`：全部 workspace 通过（Agent 53、AI 156、coding-agent 389、MCP 5、orchestrator 8、plugin-loader 17、plugin-runtime 20、plugin-sdk 1、skills 8、TUI 186；AI 真实 Provider 6 项按无凭据规则跳过）。
- `npm run test --workspace @di-code/coding-agent -- --run test/minimal-profile-process.test.ts`：1/1。
- `git diff --check`。
- `DI_CODE_PROVIDER=faux npm run dev -- --print "hello"`：输出 `Faux response`，退出码 0。

### Lifecycle/Security evidence

- Loader 动态 import 真实 namespace entry；disabled/required dependency 语义仍由 `CompositionLoader` 执行，未绕过 Loader 直接调用 plugin apply。
- `agent-loop` 只创建一个 `@di-code/agent`，并通过 Fiber disposer 解除事件订阅；session memory 的 append 在关闭后拒绝写入，dispose 幂等。
- subprocess e2e 在 stdout 断言最终 Faux 文本、退出码 0，在 trace stderr 断言 `mode-print active` 和 `session_dispose`，并确认无 required failure。
- profile 不读取 API key、不访问网络、不拼接 shell；`DI_CODE_TRACE_PLUGINS` 仅显式开启脱敏后的生命周期 JSON 诊断。插件仍为 in-process code，capability/trust 不是沙箱。

### Known risks

- 目前 CLI 仅接入 minimal print profile；interactive/json/RPC/真实 Provider 仍由 baseline 文件保留，尚未迁移到新 composition。
- Stage 6 baseline consumers still exist under `legacy-main.ts`; they are intentionally outside the Stage 7 composition and must be migrated or retired in later stages.
- `mode-print` 使用固定 Faux responses，尚未提供 profile config 文件或用户 composition layer 接入。

### Next stage context

Stage 8 应在不扩大 Bootstrap 职责的前提下加入 provider selection/config 与真实 session-store/print renderer entry，补充 profile config 发现和 disabled/optional profile e2e；随后再迁移 JSON/interactive mode。继续禁止新 profile import `legacy-main.ts` 的产品实现。

## Stage 8

### Status

passed

### Changed files

- `packages/builtins/src/index.ts`：ProviderRegistry 改为保存 Provider-neutral `Provider`/`Model`，新增 model catalog、credential-env、runtime-config/runtime-selection、provider-onboarding 和六个 Provider composition entry；Agent loop 只消费 runtime selection。
- `packages/builtins/src/{provider-openai,provider-anthropic,provider-deepseek,provider-kimi,provider-zhipu,provider-faux,model-catalog,credential-env,runtime-selection,provider-onboarding}.ts`：公开 namespace plugin subpath entries。
- `packages/builtins/package.json`：声明新增 plugin entry exports。
- `packages/builtins/README.md`：同步 Provider composition、settings/env precedence、optional credential failure 和 neutral API 边界。
- `packages/builtins/test/provider-entries.test.ts`：覆盖 credential-env redaction boundary、faux selection、unknown model。
- `packages/coding-agent/src/main.ts`：移除 faux/provider 硬编码检查；Loader profile 失败映射保持稳定用户错误。
- `packages/coding-agent/test/minimal-profile-process.test.ts`：显式固定 faux model，避免用户 settings 污染离线 smoke。
- `README.md`：更新 Stage 8 minimal profile 范围。
- `scripts/release-dry-run.mjs`：纳入新增 plugin workspaces，并在 CLI/RPC smoke 固定 faux model。
- `docs/可插拔运行时重构实施状态.md`：记录本阶段证据。

开始本阶段时 `git status --short` 为空，最近相关 commit 为 `f94d058 feat:profile（runtime + faux + agent + memory + print）`；没有用户已有改动或重叠文件。未编辑 `dist`、coverage、`.di-code`、`.env`、会话或凭据，也未自动 commit、push、reset、checkout 或删除用户文件。

### Public API

- `ProviderRegistry`、`ProviderSelection`：只暴露 `@di-code/ai` 的 Provider-neutral Provider/Model、稳定 duplicate/unknown provider/model 错误。
- `ModelCatalog`、`CredentialEnv`、`RuntimeProviderConfig`、`RuntimeSelection` 及对应 service keys。
- `@di-code/builtins/<entry>` namespace exports：`apiVersion`、`name`、`version`、`apply`。

### Composition entries

默认 print profile 按 Loader 拓扑激活 `Bootstrap`、runtime、diagnostics、process-exit、provider-registry、model-catalog、credential-env、provider-faux、provider-openai、provider-anthropic、provider-deepseek、provider-kimi、provider-zhipu、runtime-selection、provider-onboarding、session-memory、agent-loop、mode-print。真实 Provider entries 为 optional；缺少 key 时 entry failed/skipped 并保留诊断，faux 可离线启动。`main.ts`/`entry.ts` 不 import Provider adapter，只通过 Loader/HostCommandRegistry 执行。

### Tests/Checks

实际运行并通过：

- `npm run check`。
- `npm test --workspace @di-code/builtins`：2/2。
- `npm test --workspace @di-code/coding-agent -- --run test/minimal-profile-process.test.ts test/cli-process.test.ts`：8/8。
- `npm test --workspace @di-code/plugin-loader`：17/17。
- `npm test`：所有 workspace 通过（Agent 53、AI 156、builtins 2、coding-agent 389、MCP 5、orchestrator 8、plugin-loader 17、plugin-runtime 20、plugin-sdk 1、skills 8、TUI 186；AI 真实 Provider 6 项按无凭据规则跳过）。
- `npm run build`：全部 workspace 通过。
- `npm run release:dry-run`：11 packages，outside install、CLI faux 和 RPC faux smoke 通过。
- `git diff --check`。
- `DI_CODE_PROVIDER=faux DI_CODE_MODEL=faux-model npm run dev -- --print hello`：输出 `Faux response`，退出码 0。

### Lifecycle/Security evidence

- Provider adapter 只在 plugin apply 中构造，注册 owner 归属 Fiber；Loader reverse dispose 会撤销所有 entry，Agent loop 仍只有一套 `@di-code/agent` 循环。
- settings 读取只接受 JSON object；空白文件按未配置处理；global settings -> project settings 合并，`DI_CODE_PROVIDER`/`DI_CODE_MODEL` 优先；unknown provider/model fail-fast，optional provider 缺 key 不阻止 faux profile。
- credential-env 只接受完整 `$NAME`/`${NAME}` 引用，拒绝命令式凭据；错误仅包含变量名/标签，不包含 secret。Base URL 校验仍由 `@di-code/ai` adapter 执行。
- 不使用 `any`、eval 或 shell 拼接；release smoke 使用固定参数和 `--ignore-scripts` 安装。in-process plugin 仍不是 Node 沙箱，capability/trust 仅是策略和审计输入。

### Known risks

- `runtime-selection` 当前读取 global/project `settings.json` 的 provider id/model/key/baseUrl 子集；完整旧 `startup.ts` 自定义 Provider 模型 schema、locale/thinkingLevels 和 interactive onboarding 仍在 Stage 6 baseline，尚未进入新 composition。
- `provider-onboarding` entry 目前是 registry gate，尚未迁移 TTY 交互流程；JSON/interactive/RPC mode 和真实 session-store/renderer 仍未迁移。
- Provider entries 为 in-process code，缺少真实 key 时 optional failure 诊断不会提供独立进程隔离；并发 settings/registry 写入策略仍由后续阶段处理。

### Next stage context

Stage 9 应迁移完整 provider-onboarding、reasoning level/preferences、custom Provider model schema 和真实 JSONL session-store/print renderer；随后接入 JSON/interactive composition。继续保持 `main.ts` 不 import Provider files，所有 provider/config 贡献必须通过公开 ProviderRegistry 与 Loader entry 生命周期管理。

## Stage 9

### Status

blocked

### Changed files

- `packages/builtins/src/index.ts` 及新增 namespace entry：建立 `tool-registry`、`session-store-jsonl`、`session-tree`、`session-query`、`usage-meter`、`context-budget`、`compaction-basic`、`system-prompt`、`resource-loader`、`skills` 和 `agent-session` 的 composition service/entry 骨架；`agent-loop` 从 ToolRegistry snapshot 读取工具。
- `packages/builtins/package.json`：声明上述 composition entry 的公开 export。
- `packages/coding-agent/src/core/session/{types,session-storage}.ts`：Session v2 增加 `plugin` record namespace；读取、恢复和后续 append 保留未知插件记录。
- `packages/coding-agent/src/modes/tree-selector.ts`：未知 plugin record 作为不可执行的树节点显示。
- `packages/coding-agent/test/session-storage.test.ts`：覆盖未知 plugin record 的加载、保留和后续追加顺序。

开始本阶段时 `git status --short`、当前 diff 均为空；最近相关 commit 是 `2d91d82 feat: Provider 和模型迁移`。没有用户已有改动或重叠文件。未编辑 `dist`、coverage、`.di-code`、`.env`、会话或凭据；未执行 commit、push、reset、checkout 或删除操作。

### Public API

- `@di-code/builtins` 暂增上述 registry/service key 和 namespace entry export。
- Session JSONL 仍是 `SESSION_FORMAT_VERSION = 2`；新增可保留的 `{ type: "plugin", pluginId, pluginVersion, schemaVersion, data }` entry。未知记录不执行、不迁移、不丢弃。

### Composition entries

`minimalProfile` 已登记 Agent/Session/context 相关 entry，并让 `agent-loop` 读取 ToolRegistry 的稳定 snapshot。`agent-session` entry 目前只是 product-layer factory gate，尚未构造可用的 Context-backed AgentSession。

### Tests/Checks

实际运行并通过：

- `npm run check`。
- `npm test --workspace @di-code/coding-agent -- --run test/session-storage.test.ts`：22/22。
- `npm test --workspace @di-code/builtins`：2/2。
- `npm run build --workspace @di-code/builtins`。

### Lifecycle/Security evidence

- plugin record 解码要求稳定 record id、父节点、ISO 时间、非空 plugin id/version、安全整数 schemaVersion 和 JSON-serializable data；坏 record 延续现有 diagnostics/recovery 行为。
- JSONL append 仍在原有并发锁和 expected-parent 校验后进行；测试确认未知 plugin record 在读取和后续 append 中保持物理顺序。
- 未增加 `any`、`eval`、shell 拼接、凭据读取或文件删除。

### Known risks

- **阻塞门：**`packages/coding-agent/src/core/session.ts` 的 `AgentSession` 仍创建 read/write/edit/bash/glob/grep/load-skill 内建工具数组，并接收/调用 `ExtensionHost`。大量 legacy mode、RPC 和测试直接使用该构造器。当前 registry 骨架没有完成“AgentSession 仅从 Context 获得 Provider/Tool/Session/Prompt/Compaction snapshot”的迁移，故不能标记 passed。
- `session-store-jsonl`、system prompt、resource loader、skills 和 compaction entry 尚未接入真实 product SessionContext；继续接线前需先定义 SessionFactory 输入、snapshot ownership、dispose 和 legacy baseline 的显式隔离策略。

### Next stage context

继续 Stage 9 前，先将 AgentSession 的构造和生命周期迁入 `SessionContext`：删除其内建工具创建和 `ExtensionHost` 依赖，以 ToolRegistry/ProviderRegistry/SessionStoreRegistry/PromptRegistry/CompactionRegistry 的不可变 snapshot 组装唯一 `@di-code/agent` loop。随后把现有 JSONL manager、resources/system prompt、skills、usage 与 compaction adapter 放入各自 entry，补 Context isolation、plugin record 保留、tree/compaction 和真实 CLI session smoke；全部通过后再将本 Stage 状态改为 `passed`。

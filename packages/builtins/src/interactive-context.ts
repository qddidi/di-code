import { interactiveContext } from "./index.ts";
export const apiVersion = interactiveContext.apiVersion;
export const name = interactiveContext.name;
export const version = interactiveContext.version;
export const apply = interactiveContext.apply;

import { workspace } from "./index.ts";
export const apiVersion = workspace.apiVersion;
export const name = workspace.name;
export const version = workspace.version;
export const apply = workspace.apply;

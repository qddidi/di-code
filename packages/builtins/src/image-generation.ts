import { imageGeneration } from "./index.ts";

export const apiVersion = imageGeneration.apiVersion;
export const name = imageGeneration.name;
export const version = imageGeneration.version;
export const apply = imageGeneration.apply;

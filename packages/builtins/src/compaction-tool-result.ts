import { compactionToolResult } from "./index.ts";
export const apiVersion = compactionToolResult.apiVersion;
export const name = compactionToolResult.name;
export const version = compactionToolResult.version;
export const apply = compactionToolResult.apply;

import { modeInteractive } from "./index.ts";
export const apiVersion = modeInteractive.apiVersion;
export const name = modeInteractive.name;
export const version = modeInteractive.version;
export const apply = modeInteractive.apply;

import { commandCompact } from "./index.ts";
export const apiVersion = commandCompact.apiVersion;
export const name = commandCompact.name;
export const version = commandCompact.version;
export const apply = commandCompact.apply;

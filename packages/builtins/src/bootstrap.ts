import { bootstrap } from "./index.ts";
export const apiVersion = bootstrap.apiVersion;
export const name = bootstrap.name;
export const version = bootstrap.version;
export const apply = bootstrap.apply;

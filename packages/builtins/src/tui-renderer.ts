import { tuiRenderer } from "./index.ts";
export const apiVersion = tuiRenderer.apiVersion;
export const name = tuiRenderer.name;
export const version = tuiRenderer.version;
export const apply = tuiRenderer.apply;

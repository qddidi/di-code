import { systemPrompt } from "./index.ts";
export const apiVersion = systemPrompt.apiVersion;
export const name = systemPrompt.name;
export const version = systemPrompt.version;
export const apply = systemPrompt.apply;

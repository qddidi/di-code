import { resourceLoader } from "./index.ts";
export const apiVersion = resourceLoader.apiVersion;
export const name = resourceLoader.name;
export const version = resourceLoader.version;
export const apply = resourceLoader.apply;

import { toolWrite } from "./index.ts";
export const apiVersion = toolWrite.apiVersion;
export const name = toolWrite.name;
export const version = toolWrite.version;
export const apply = toolWrite.apply;

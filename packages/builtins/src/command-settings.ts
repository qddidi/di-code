import { commandSettings } from "./index.ts";
export const apiVersion = commandSettings.apiVersion;
export const name = commandSettings.name;
export const version = commandSettings.version;
export const apply = commandSettings.apply;

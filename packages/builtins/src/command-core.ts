import { commandCore } from "./index.ts";
export const apiVersion = commandCore.apiVersion;
export const name = commandCore.name;
export const version = commandCore.version;
export const apply = commandCore.apply;

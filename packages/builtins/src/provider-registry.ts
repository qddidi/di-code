import { providerRegistry } from "./index.ts";
export const apiVersion = providerRegistry.apiVersion;
export const name = providerRegistry.name;
export const version = providerRegistry.version;
export const apply = providerRegistry.apply;

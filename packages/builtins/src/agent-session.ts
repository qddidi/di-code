import { agentSession } from "./index.ts";
export const apiVersion = agentSession.apiVersion;
export const name = agentSession.name;
export const version = agentSession.version;
export const apply = agentSession.apply;

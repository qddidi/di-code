import { usageMeter } from "./index.ts";
export const apiVersion = usageMeter.apiVersion;
export const name = usageMeter.name;
export const version = usageMeter.version;
export const apply = usageMeter.apply;

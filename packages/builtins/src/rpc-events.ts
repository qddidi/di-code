import { rpcEvents } from "./index.ts";

export const apiVersion = rpcEvents.apiVersion;
export const name = rpcEvents.name;
export const version = rpcEvents.version;
export const apply = rpcEvents.apply;

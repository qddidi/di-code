import { compactionBasic } from "./index.ts";
export const apiVersion = compactionBasic.apiVersion;
export const name = compactionBasic.name;
export const version = compactionBasic.version;
export const apply = compactionBasic.apply;

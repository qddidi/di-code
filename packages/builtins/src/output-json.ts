import { outputJson } from "./index.ts";
export const apiVersion = outputJson.apiVersion;
export const name = outputJson.name;
export const version = outputJson.version;
export const apply = outputJson.apply;

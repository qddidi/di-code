import { providerOnboarding } from "./index.ts";
export const apiVersion = providerOnboarding.apiVersion;
export const name = providerOnboarding.name;
export const version = providerOnboarding.version;
export const apply = providerOnboarding.apply;

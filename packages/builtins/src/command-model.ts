import { commandModel } from "./index.ts";
export const apiVersion = commandModel.apiVersion;
export const name = commandModel.name;
export const version = commandModel.version;
export const apply = commandModel.apply;

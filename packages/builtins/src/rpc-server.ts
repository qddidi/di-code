import { rpcServer } from "./index.ts";

export const apiVersion = rpcServer.apiVersion;
export const name = rpcServer.name;
export const version = rpcServer.version;
export const apply = rpcServer.apply;

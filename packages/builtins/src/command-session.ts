import { commandSession } from "./index.ts";
export const apiVersion = commandSession.apiVersion;
export const name = commandSession.name;
export const version = commandSession.version;
export const apply = commandSession.apply;

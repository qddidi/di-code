import { sessionMemory } from "./index.ts";
export const apiVersion = sessionMemory.apiVersion;
export const name = sessionMemory.name;
export const version = sessionMemory.version;
export const apply = sessionMemory.apply;

import { cliParser } from "./index.ts";
export const apiVersion = cliParser.apiVersion;
export const name = cliParser.name;
export const version = cliParser.version;
export const apply = cliParser.apply;

import { toolPolicy } from "./index.ts";
export const apiVersion = toolPolicy.apiVersion;
export const name = toolPolicy.name;
export const version = toolPolicy.version;
export const apply = toolPolicy.apply;

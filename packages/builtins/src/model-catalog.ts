import { modelCatalog } from "./index.ts";
export const apiVersion = modelCatalog.apiVersion;
export const name = modelCatalog.name;
export const version = modelCatalog.version;
export const apply = modelCatalog.apply;

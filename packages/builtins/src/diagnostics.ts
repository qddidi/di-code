import { diagnostics } from "./index.ts";
export const apiVersion = diagnostics.apiVersion;
export const name = diagnostics.name;
export const version = diagnostics.version;
export const apply = diagnostics.apply;

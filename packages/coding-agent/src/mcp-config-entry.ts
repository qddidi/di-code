import { mcpConfig } from "./mcp/entries.ts";
export const apiVersion = mcpConfig.apiVersion;
export const name = mcpConfig.name;
export const version = mcpConfig.version;
export const apply = mcpConfig.apply;
